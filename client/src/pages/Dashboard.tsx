import { useEffect, useMemo, useState, type ReactNode } from "react";
import { ArrowLeft, ArrowUpRight, Bookmark, BookOpen, CircleUserRound, FilePenLine, Heart, LogIn, PackageCheck, PenLine, Plus, Sparkles } from "lucide-react";
import { onAuthStateChanged, type User } from "firebase/auth";
import { addDoc, collection, doc, onSnapshot, query, serverTimestamp, setDoc, where, type DocumentData } from "firebase/firestore";
import { toast } from "sonner";
import AuthButton from "@/components/AuthButton";
import ProposalEntry from "@/components/ProposalEntry";
import SiteFooter from "@/components/SiteFooter";
import { auth, db } from "@/lib/firebase";

/** Apple 式產品敘事提醒：會員工作台不用密集儀表板，而以大標、清楚分段與淺灰資訊表面，讓收藏、支持與提案各自有呼吸空間。 */

type BookmarkRecord = { id: string; title: string; projectSlug?: string; category?: string; addedAt?: { toDate?: () => Date } };
type PledgeRecord = { id: string; projectTitle?: string; rewardTitle?: string; amount?: number; status?: string };
type ProposalRecord = { id: string; title?: string; format?: string; status?: string; updatedAt?: { toDate?: () => Date } };
const basePath = import.meta.env.BASE_URL;
const facebookUrl = "https://www.facebook.com/share/19TwFNqfoG/?mibextid=wwXIfr";
const projectPath = (slug: string) => `${basePath}project/${slug}`;
const formatDate = (value?: { toDate?: () => Date }) => value?.toDate?.().toLocaleDateString("zh-TW", { month: "short", day: "numeric" }) ?? "尚未記錄";
const recordFromSnapshot = <T,>(snapshot: { id: string; data: () => DocumentData }) => ({ id: snapshot.id, ...snapshot.data() }) as T;

function LoadingPanel() { return <div className="grid min-h-screen place-items-center bg-white text-sm text-[#6e6e73]"><div className="flex items-center gap-3"><span className="size-5 animate-spin rounded-full border-2 border-[#d2d2d7] border-t-[#f36b3b]"/>正在開啟你的工作台</div></div>; }
function SignedOutPanel() { return <div className="min-h-screen bg-white text-[#1d1d1f]"><main className="container grid min-h-[78vh] place-items-center py-12"><div className="max-w-md text-center"><div className="mx-auto grid size-14 place-items-center rounded-[20px] bg-[#f5f5f7] text-[#172846]"><LogIn size={24}/></div><p className="odj-eyebrow mt-7 text-[#6e6e73]">MEMBER ACCESS</p><h1 className="font-display mt-3 text-4xl tracking-[-0.05em]">先登入，再開始。</h1><p className="mt-4 text-sm leading-7 text-[#6e6e73]">工作台會保留你的帳號、收藏、支持紀錄與提案草稿。未登入者將返回首頁。</p><div className="mt-7"><ProposalEntry fullWidth label="使用 Google 登入" loginLabel="使用 Google 登入" /></div><a href={basePath} className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[#172846] transition hover:text-[#f36b3b]"><ArrowLeft size={16}/> 回到首頁</a></div></main><SiteFooter /></div>; }
function Panel({ eyebrow, title, icon, action, children }: { eyebrow: string; title: string; icon: ReactNode; action?: ReactNode; children: ReactNode }) { return <section className="rounded-[28px] bg-[#f5f5f7] p-6 sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="odj-eyebrow text-[#6e6e73]">{eyebrow}</p><div className="mt-3 flex items-center gap-2"><span className="text-[#172846]">{icon}</span><h2 className="font-display text-2xl tracking-[-0.03em]">{title}</h2></div></div>{action}</div><div className="mt-6">{children}</div></section>; }

export default function Dashboard() {
  const [user, setUser] = useState<User | null>(null);
  const [ready, setReady] = useState(false);
  const [bookmarks, setBookmarks] = useState<BookmarkRecord[]>([]);
  const [pledges, setPledges] = useState<PledgeRecord[]>([]);
  const [proposals, setProposals] = useState<ProposalRecord[]>([]);
  const [creatingDraft, setCreatingDraft] = useState(false);
  useEffect(() => onAuthStateChanged(auth, (nextUser) => { setUser(nextUser); setReady(true); }), []);
  useEffect(() => { if (!ready || user) return; const timeout = window.setTimeout(() => window.location.assign(basePath), 1800); return () => window.clearTimeout(timeout); }, [ready, user]);
  useEffect(() => {
    if (!user) return;
    void setDoc(doc(db, "users", user.uid), { displayName: user.displayName || null, email: user.email || null, photoURL: user.photoURL || null, updatedAt: serverTimestamp(), createdAt: serverTimestamp() }, { merge: true }).catch(() => toast("會員資料暫時無法同步", { description: "請稍後重新整理；你的 Google 登入狀態不受影響。" }));
    const stopBookmarks = onSnapshot(collection(db, "bookmarks", user.uid, "items"), (snapshot) => setBookmarks(snapshot.docs.map((item) => recordFromSnapshot<BookmarkRecord>(item))), () => toast("目前無法讀取收藏", { description: "請確認網路狀態後再試一次。" }));
    const stopPledges = onSnapshot(collection(db, "pledges", user.uid, "records"), (snapshot) => setPledges(snapshot.docs.map((item) => recordFromSnapshot<PledgeRecord>(item))), () => toast("目前無法讀取支持紀錄", { description: "正式贊助開放後，紀錄會在這裡顯示。" }));
    const stopProposals = onSnapshot(query(collection(db, "proposals"), where("authorUid", "==", user.uid)), (snapshot) => setProposals(snapshot.docs.map((item) => recordFromSnapshot<ProposalRecord>(item)).sort((a, b) => (b.updatedAt?.toDate?.().getTime() ?? 0) - (a.updatedAt?.toDate?.().getTime() ?? 0))), () => toast("目前無法讀取提案草稿", { description: "請確認登入狀態後重新整理。" }));
    return () => { stopBookmarks(); stopPledges(); stopProposals(); };
  }, [user]);
  const displayName = useMemo(() => user?.displayName || user?.email?.split("@")[0] || "老東家會員", [user]);
  const initial = displayName.slice(0, 1).toUpperCase();
  const createDraft = async () => { if (!user || creatingDraft) return; setCreatingDraft(true); try { await addDoc(collection(db, "proposals"), { authorUid: user.uid, title: "未命名提案", format: "小說或衍生周邊", status: "draft", createdAt: serverTimestamp(), updatedAt: serverTimestamp() }); toast("已建立提案草稿", { description: "提案工作區正在建置中；草稿已安全儲存在你的帳號下。" }); } catch { toast("建立草稿失敗", { description: "請確認登入狀態與網路連線後再試一次。" }); } finally { setCreatingDraft(false); } };
  if (!ready) return <LoadingPanel />;
  if (!user) return <SignedOutPanel />;
  return <div className="min-h-screen overflow-x-hidden bg-white text-[#1d1d1f]"><header className="sticky top-0 z-40 border-b border-[#d2d2d7]/80 bg-white/80 backdrop-blur-xl"><div className="container flex h-[52px] items-center justify-between gap-4"><a href={basePath} className="group inline-flex items-center gap-2.5"><img src={`${basePath}media/odj-sunrise-icon.png`} alt="老東家日出圖示" className="size-9 rounded-[12px] object-cover transition group-hover:-translate-y-0.5"/><span className="font-display text-[18px] tracking-[-0.02em]">老東家</span></a><div className="flex items-center gap-4"><a href={basePath} className="hidden text-xs font-medium text-[#424245] transition hover:text-[#f36b3b] sm:block">探索作品</a><a href={facebookUrl} target="_blank" rel="noreferrer" className="hidden text-xs font-medium text-[#424245] transition hover:text-[#f36b3b] sm:block">Facebook</a><AuthButton compact /></div></div></header><main><section className="bg-[#f5f5f7]"><div className="container py-16 sm:py-20"><div className="mx-auto max-w-4xl text-center"><p className="odj-eyebrow text-[#6e6e73]">MEMBER WORKBENCH</p><h1 className="font-display mt-4 text-4xl tracking-[-0.055em] sm:text-6xl">早安，{displayName}。</h1><p className="mx-auto mt-5 max-w-2xl text-base leading-8 text-[#6e6e73]">收藏、支持與創作草稿都在這裡。每一個下一步，都保持在看得見的位置。</p></div><div className="mx-auto mt-10 flex max-w-md items-center gap-4 rounded-[24px] bg-white p-4 odj-shadow"><div className="grid size-12 shrink-0 place-items-center overflow-hidden rounded-full bg-[#172846] text-lg font-semibold text-white">{user.photoURL ? <img src={user.photoURL} alt="會員頭像" className="h-full w-full object-cover" referrerPolicy="no-referrer"/> : initial}</div><div className="min-w-0 text-left"><p className="truncate font-semibold">{displayName}</p><p className="mt-1 truncate text-sm text-[#6e6e73]">{user.email}</p></div></div></div></section><section className="container py-12 sm:py-16"><div className="grid gap-6 lg:grid-cols-[.82fr_1.18fr]"><div className="space-y-6"><Panel eyebrow="帳號" title="我的帳號" icon={<CircleUserRound size={20}/>}><div className="space-y-3 text-sm"><div className="flex justify-between gap-4 border-b border-[#d2d2d7] pb-3"><span className="text-[#6e6e73]">登入方式</span><span className="font-semibold">Google</span></div><div className="flex justify-between gap-4"><span className="text-[#6e6e73]">會員識別</span><span className="max-w-[170px] truncate text-xs text-[#424245]" title={user.uid}>{user.uid}</span></div></div></Panel><Panel eyebrow="創作者" title="開始一份提案" icon={<Sparkles size={20}/>} action={<PenLine size={19} className="text-[#f36b3b]"/>}><p className="text-sm leading-7 text-[#6e6e73]">提案草稿只會對登入帳號本人開放。先建立骨架，下一步再補上作品內容與回饋方案。</p><button onClick={createDraft} disabled={creatingDraft} className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-[#f36b3b] px-4 py-3 text-sm font-semibold text-white transition hover:bg-[#d9522d] active:scale-[0.97] disabled:cursor-wait disabled:opacity-70">{creatingDraft ? <span className="size-4 animate-spin rounded-full border-2 border-white border-t-transparent"/> : <Plus size={17}/>} {creatingDraft ? "正在建立草稿" : "建立提案草稿"}</button></Panel></div><div className="space-y-6"><Panel eyebrow="提案草稿" title="我的作品" icon={<FilePenLine size={20}/>} action={<span className="text-sm font-semibold text-[#f36b3b]">{proposals.length.toString().padStart(2, "0")}</span>}>{proposals.length ? <div className="space-y-3">{proposals.map((proposal) => <div key={proposal.id} className="flex items-center justify-between gap-4 rounded-2xl bg-white p-4"><div className="min-w-0"><p className="truncate font-semibold">{proposal.title || "未命名提案"}</p><p className="mt-1 text-xs text-[#6e6e73]">{proposal.format || "小說或衍生周邊"} · 上次編輯 {formatDate(proposal.updatedAt)}</p></div><span className="shrink-0 text-xs font-semibold text-[#f36b3b]">草稿</span></div>)}</div> : <div className="rounded-2xl bg-white p-5 text-sm leading-7 text-[#6e6e73]">還沒有提案草稿。從左側建立第一份草稿，資料會鎖定在你的帳號下。</div>}</Panel><div className="grid gap-6 md:grid-cols-2"><Panel eyebrow="收藏" title="想再讀一次" icon={<Bookmark size={20}/>} action={<Heart size={18} className="text-[#f36b3b]"/>}>{bookmarks.length ? <div className="space-y-3">{bookmarks.map((bookmark) => <a key={bookmark.id} href={bookmark.projectSlug ? projectPath(bookmark.projectSlug) : basePath} className="group flex items-center justify-between gap-3 rounded-2xl bg-white p-4 transition hover:bg-[#fff0e8]"><div className="min-w-0"><p className="truncate text-sm font-semibold group-hover:text-[#f36b3b]">{bookmark.title || "未命名收藏"}</p><p className="mt-1 text-xs text-[#6e6e73]">{bookmark.category || "專案"} · 收藏於 {formatDate(bookmark.addedAt)}</p></div><ArrowUpRight size={16} className="shrink-0 text-[#6e6e73]"/></a>)}</div> : <p className="rounded-2xl bg-white p-5 text-sm leading-7 text-[#6e6e73]">尚未收藏故事。從首頁開始，把想再讀一次的提案留在這裡。</p>}<a href={basePath} className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[#172846] transition hover:text-[#f36b3b]">探索作品 <ArrowUpRight size={15}/></a></Panel><Panel eyebrow="支持" title="支持紀錄" icon={<PackageCheck size={20}/>} action={<BookOpen size={18} className="text-[#6e6e73]"/>}>{pledges.length ? <div className="space-y-3">{pledges.map((pledge) => <div key={pledge.id} className="rounded-2xl bg-white p-4"><p className="text-sm font-semibold">{pledge.projectTitle || "支持專案"}</p><p className="mt-1 text-xs text-[#6e6e73]">{pledge.rewardTitle || "回饋方案"} · NT$ {pledge.amount?.toLocaleString() || "—"} · {pledge.status || "處理中"}</p></div>)}</div> : <p className="rounded-2xl bg-white p-5 text-sm leading-7 text-[#6e6e73]">目前沒有支持紀錄。老東家仍是原型，尚未開放真實贊助與付款。</p>}</Panel></div></div></div></section></main><SiteFooter /></div>;
}
