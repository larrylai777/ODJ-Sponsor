# ODJ Sponsor 會員工作台發布紀錄

日期：2026-08-17

本次發布已將會員工作台、提案入口的 Google 登入融合、Firestore 會員資料讀取與提案草稿建立流程推送至 `main` 與 `gh-pages` 分支。GitHub Pages 的深層入口已包含 `dashboard/`、`about/`、`privacy/`、`terms/` 與三個展示專案頁。

公開網址首次檢查時，`https://larrylai777.github.io/ODJ-Sponsor/dashboard/` 仍回傳 GitHub Pages 404；這代表公開發布尚在傳播或快取更新期間，並不影響已推送至 `gh-pages` 分支的 `dashboard/index.html`。應在發布傳播完成後再以公開網址複查。
